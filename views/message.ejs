<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Регистрация</title>
    <link rel="icon" href="/static/gg.png">
    <script defer src="http://localhost:3000/socket.io/socket.io.js"></script>
    <script defer src="/static/client.js"></script>
    <style>
        table {width: 100%; border-collapse: collapse;}
        table td {padding: 12px 16px;}
        table thead tr {font-weight: bold; border-top: 1px solid #e8e9eb;}
        table tr {border-bottom: 1px solid #e8e9eb;}
        table tbody tr:hover {background: #e8f6ff;}

        body{
            margin:  0;
            padding: 0;
            font-family:     sans-serif;
            background-color: lightblue;
            background-size:      cover;
            background-position: center;
            background-image: url('/static/son.jpg');
        }
        .sign-up-form
        {
            width: 400px;
            box-shadow: 0 0 3px 0 rgba(0,0, 0, 0.3);
            background: #fff;
            padding: 20px;
            margin: 8% auto 5%;
            text-align: center;
            border-radius: 20px;
        }
        sign-up-form h1{
            color: #1c8adb;
            margin-bottom: 30px;
        }
        .input-box{
            border-radius: 20px;
            padding:       10px;
            margin:      10px 0;
            width: 90%;
            border: 1px solid #999;
            outline: none;
        }
        button{
            color: #fff;
            width: 100%;
            padding: 10px;
            border-radius: 20px;
            font-size: 15px;
            margin: 10px 0;
            border: none;
            outline: none;
            cursor: pointer;
        }
        hr{
            margin-top: 20px;
            width: 80%;
        }
        .or{
            background: #fff;
            width: 30px;
            margin: -19px auto;
        }
        .signup-btn{
            background-color: #1c8adb;
        }
        .sign-up-form h1 {
            color: #1c8adb;
            margin-bottom: 30px;
        }
        img{
            width:150px;
            height:100px;
        }
        .btn{
            background: #1c8adb;
            color: white;
        }
        .btn:hover {
            color: black;
        }
        .input-box{
        }

    </style>
    <script src = "/socket.io/socket.io.js"></script>
    </script>
</head>
<body>
<%-include('navbar')%>
<% function getmon(month){%>
    <%    let mon = ["January","February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]%>
<%      return mon[month]}%>
<div class="sign-up-form" style="width: 1000px;;">
    <h1 style="margin: 0">Send message to<%=toUser.name%></h1>
    <div class="cont" style="display: flex; margin-bottom:-10%" >
        <div class="sign-up-form" style="width: 200px; height: 200px">
            <img src="/static/<%=toUser.avatar%>">
            <a href="/profile/<%=toUser.name%>" style="margin: 15%" class="btn"><%=toUser.name%></a>
        </div>
        <div class="sign-up-form" >
            <p><%=toUser.name%>'s messages</p>
            <table>
                <tbody>
                <%let i;%>
                <%if(messages.length>=5){i = messages.length - 5} else{i = 0}%>
                <%for(i; i < messages.length;i++){%>
                <tr>
                    <td><%=messages[i].text%></td>
                    <td><p style="opacity: 50%;"><%=messages[i].date.getDay()+1 + " " + getmon(messages[i].date.getMonth()) + " " + messages[i].date.getHours() + ":" + messages[i].date.getMinutes()%></p></td>
                </tr>
                <%}%>
                </tbody>
            </table>
        </div>
        <div class="sign-up-form" >
            <p>My messages</p>
            <table>

                <tbody>
                <%let a;%>
                <%if(mymes.length>=5){a = mymes.length - 5} else{a = 0}%>
                <%for(a; a < mymes.length;a++){%>
                <tr>
                    <td><%=mymes[a].text%></td>
                    <td><p style="opacity: 50%;"><%=mymes[a].date.getDay()+1 + " " + getmon(mymes[a].date.getMonth()) + " " + mymes[a].date.getHours() + ":" + mymes[a].date.getMinutes()%></p></td>
                </tr>
                <%}%>
                </tbody>
            </table>
        </div>
    </div>
    <div class="sign-up-form" style="width: 100%">
        <form action="" method="POST">
            <input type="text"
                   id="text"
                   name="text"
                   class="input-box">
            <hr>
            <button type="submit" class="signup-btn">Send </button>
        </form>
    </div>
    </div>
    </div>

</div>

<%-include('footer')%>
</body>
</html>